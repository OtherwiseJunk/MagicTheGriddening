# PostgreSQL connection string — must point to the same database used by MagicTheGriddening
DATABASE_URL=

# Minimum number of Scryfall results required for a puzzle intersection to be valid (default: 10)
# MINIMUM_HITS=10

# Set to any value to run a single generation and exit (no cron scheduler)
# RUN_ONCE=true
